domain = "architecture"
schema_version = 1

[component.atrament_browser_launch]
depends_on = []

[component.atrament_diagnostic]
depends_on = []

[component.atrament_physical_page_profile]
depends_on = []

[component.atrament_semantic_notebook]
depends_on = ["atrament_physical_page_profile"]

[component.atrament_semantic_notebook_port]
depends_on = ["atrament_physical_page_profile", "atrament_semantic_notebook"]

[component.atrament_semantic_notebook_session]
depends_on = ["atrament_semantic_notebook", "atrament_semantic_notebook_port"]

[component.atrament_runtime_bootstrap]
depends_on = ["atrament_browser_launch", "atrament_session_draft", "atrament_session_handshake", "atrament_session_runtime", "atrament_session_secret"]

[component.atrament_session_draft]
depends_on = ["atrament_diagnostic", "atrament_session_draft_port"]

[component.atrament_session_draft_port]
depends_on = ["atrament_diagnostic"]

[component.atrament_session_handshake]
depends_on = ["atrament_diagnostic", "atrament_session_handshake_port"]

[component.atrament_session_handshake_port]
depends_on = ["atrament_diagnostic"]

[component.atrament_session_runtime]
depends_on = ["atrament_diagnostic", "atrament_session_draft_port", "atrament_session_handshake_port"]

[component.atrament_session_secret]
depends_on = []
