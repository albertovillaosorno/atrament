domain = "integrations"
schema_version = 1

[graph]
authority_roots = [".jig"]
cache_root = ".cache/jig/graph"
excluded_roots = [".cache", ".dependencies", ".git", ".logs", ".temp", "target"]
taxonomy_path = ".jig/taxonomy.json"
