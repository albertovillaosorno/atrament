domain = "linters"
schema_version = 1

[applicability.clang-format]
enabled = false
reason = "The planning generation contains no C, C++, or CUDA source."

[applicability.clang-tidy]
enabled = false
reason = "The planning generation contains no C, C++, or CUDA source."

[applicability.clippy-config]
enabled = true
reason = "The Rust session runtime is governed by explicit Clippy policy."

[applicability.clippy-lints]
enabled = false
reason = "Repository-local Rust exists, but no RustClippy native validation capability is configured yet."

[applicability.coverage-py]
enabled = false
reason = "The planning generation has no Python coverage target."

[applicability.csharp-global]
enabled = false
reason = "The repository contains no C# source."

[applicability.csharp-visualstudio]
enabled = false
reason = "The repository contains no C# source."

[applicability.directory-build]
enabled = false
reason = "The repository has no MSBuild authority."

[applicability.editorconfig]
enabled = true
reason = "EditorConfig governs repository-authored text surfaces."

[applicability.msvc-cpp]
enabled = false
reason = "The planning generation contains no C or C++ source."

[applicability.pyright]
enabled = false
reason = "The planning generation contains no Python source."

[applicability.pytest]
enabled = false
reason = "The planning generation contains no Python tests."

[applicability.ruff]
enabled = false
reason = "The planning generation contains no Python source."

[applicability.rust-toolchain]
enabled = true
reason = "The Rust session runtime requires a pinned toolchain policy."

[applicability.rustc-lints]
enabled = false
reason = "Stable Cargo is repository-local, but no RustCheck native validation capability is configured yet."

[applicability.rustfmt]
enabled = false
reason = "Repository-local Rust exists, but no Rustfmt native validation capability is configured yet."

[applicability.typescript]
enabled = true
reason = "The browser scaffold is governed by reviewed TypeScript policy."

[applicability.unreal]
enabled = false
reason = "The repository is not an Unreal Engine project."
