[package]
name = "atrament_citation_review_linkage"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT"

[dependencies]
atrament_semantic_notebook = { path = "../semantic-notebook" }

[lib]
path = "domain/lib.rs"

[lints]
workspace = true
