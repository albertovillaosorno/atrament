[package]
name = "atrament_mcp_semantic_command_admission_projection"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT"

[dependencies]
atrament_mcp_capability_effect = { path = "../mcp-capability-effect" }
atrament_mcp_effect_admission = { path = "../mcp-effect-admission" }
atrament_semantic_command_context = { path = "../semantic-command-context" }
atrament_semantic_notebook_port = { path = "../semantic-notebook-port" }

[lib]
path = "application/lib.rs"

[lints]
workspace = true
