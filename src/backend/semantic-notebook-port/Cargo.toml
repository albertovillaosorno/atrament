[package]
name = "atrament_semantic_notebook_port"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT"

[dependencies]
atrament_physical_page_profile = { path = "../physical-page-profile" }
atrament_semantic_notebook = { path = "../semantic-notebook" }

[lib]
path = "port-inbound/lib.rs"

[lints]
workspace = true
