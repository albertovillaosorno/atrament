[package]
name = "atrament_session_handshake_port"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT"

[dependencies]
atrament_diagnostic = { path = "../diagnostic" }

[lib]
path = "port-inbound/lib.rs"

[lints]
workspace = true
