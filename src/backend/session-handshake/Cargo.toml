[package]
name = "atrament_session_handshake"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT"

[dependencies]
atrament_diagnostic = { path = "../diagnostic" }
atrament_one_shot_formatting_prompt = { path = "../one-shot-formatting-prompt" }
atrament_session_handshake_port = { path = "../session-handshake-port" }

[dependencies.atrament_handwriting_profile_container]
path = "../handwriting-profile-container"

[lib]
path = "application/lib.rs"

[lints]
workspace = true
