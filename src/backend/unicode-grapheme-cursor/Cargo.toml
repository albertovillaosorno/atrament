[package]
name = "atrament_unicode_grapheme_cursor"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT"

[dependencies]
atrament_unicode_grapheme_boundary_port = { path = "../unicode-grapheme-boundary-port" }

[lib]
path = "application/lib.rs"

[lints]
workspace = true
